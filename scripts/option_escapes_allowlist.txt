# Allowlist for elaboration-budget escape hatches — enforced by scripts/check_option_escapes.sh.
#
# Format:  <option>  <path>  <value>  <count>   # justification
#
# The key is (option, path, value) and the count must match EXACTLY. Any site not listed here fails
# the build; so does raising a listed value, or adding a second site at the same value to a listed
# file. This is a PROHIBITION with named exceptions, not a budget.
#
# ⚠ THIS LIST IS A LAST RESORT. It is not a place to record that something is slow. Hand-written Lean
# in this repo carries ZERO `maxHeartbeats`; every entry below is a generated definition or a measured
# structural case, and each states its floor bracket and its mechanism.
#
# Before adding an entry, read docs/agents/proof-patterns.md — extract over OPAQUE arguments, and
# check what the extraction can still see. Then meet the documented bar: a measured floor bracket
# from a real ladder, a named mechanism, at least one attempted fix with its measured result, and a
# reason the cause cannot be moved (not merely that a first attempt did not move it). If the site is
# generated, fix the emitter in `update_extracted.py` instead — its blanket emission has
# repeatedly proven one to two orders of magnitude over what any definition actually needed.
#
# For calibration: upstream Clean has ZERO `maxHeartbeats` in 44,603 lines and 4 `maxRecDepth` sites,
# all in constant tables, capped at 2048, none in a proof.
#
# RE-MEASURED at Lean v4.33.1 / Clean main (2026-09-20, the 4.32.2 -> 4.33.1 migration). The
# `Faithful/BranchChip.lean` `maxRecDepth` entry left then: its closing `ring_nf` over the whole
# assertion list (6.5 s, over the heartbeat budget on 4.33) became eight element-wise `ring`s
# (0.3 s, no option). Every remaining site was re-tested by STRIPPING its option and re-elaborating.
# (That check matters because this gate is exact set equality — a site the toolchain no longer
# needs FAILS the build until its line is deleted.)
#
# ---- maxHeartbeats: 1 site, on a GENERATED definition ------------------------------------------
# Emitted by update_extracted.py and reproduced by a regeneration; fixes go in the generator, never
# by editing the file.

# The only generated AIR definition that needs any budget. Fails in `«LCNF simp»` — the CODE GENERATOR,
# not elaboration; the elaborator handles every generated def at the plain default. Ladder: 200000 FAIL
# / 210000 ok → floor (200000, 210000], barely over the default; set ~2x because codegen cost tracks
# toolchain changes less predictably than elaboration. Removable only via `noncomputable`, which
# cascades into hand-written Faithful/ files; that trade is deliberately declined.
maxHeartbeats  SP1Clean/Extracted/ChipOracle/DivRem.lean            400000   1

# (Retired 2026-09-22: `SystemOracle/Global.lean`'s file-level 1000000. Its `assertsPart*` chains —
# one Poseidon-style entry with a 1239-binding `let` closure — are now hoisted by the emitter into
# one top-level def per binding (`_hoist_let_chains` in update_extracted.py): elaboration is linear
# again, 62 s / 14 GB → 7 s / 3 GB for the module, and no budget is needed.)

# ---- maxRecDepth: 1 site (Lean's default is 512) ------------------------------------------------

# (Retired 2026-09-22 with the heartbeat entry above: Global's four `assertsPart*` sites at 4000 —
# the recursion was the nested `let` chain, gone with the hoisting.)

# `deriving instance Fintype for Register` — recurses once per constructor and just misses the default.
# Bracket (512, 600], the narrowest in the tree; set to 800 rather than 600 because 17% over a measured
# failing value is not a margin. Scoped, not file-level: a file-level bump silently covers declarations
# that never needed it.
maxRecDepth    SP1Clean/Model/Machine/ConfiguredState.lean                  800   1


# ---- backward.isDefEq.respectTransparency: 1 site --------------------------------------------------

# The v4.33 unifier checks metavariable assignments at implicit transparency (Lean #13895). Mathlib's
# `Fintype` deriving handler closes its `complete` field by a `rw` that is well-typed only up to
# unfolding `Multiset.Nodup`, so the derive fails for every enum; Mathlib's own
# `MathlibTest/DeriveFintype.lean` disables the check around each derive exactly like this. Scoped to
# the one `deriving instance Fintype for Register`; goes with the upstream handler fix.
backward.isDefEq.respectTransparency  SP1Clean/Model/Machine/ConfiguredState.lean  false  1
